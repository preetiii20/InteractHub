server.port=8090
spring.application.name=notification-service

# --- FINAL, STABLE GMAIL SMTP CONFIGURATION (SSL/Port 465) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Your actual Gmail address (CRITICAL: MUST be correct)
spring.mail.username=preetik2172@gmail.com

# Your 16-character App Password (CRITICAL: MUST be correct)
spring.mail.password=eyam fspw ylyi lcwp

# CRITICAL OVERRIDES for SSL:
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.port=465 
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory 
spring.mail.properties.mail.smtp.socketFactory.fallback=false

# --- REMOVE OR COMMENT OUT STARTTLS LINES ---
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true